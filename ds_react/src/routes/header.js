import React from 'react';
import "scss/header.scss";
const Header = () => {
    return (
        <header>
            <a href="home">
                DS_BLOG
            </a>
        </header>
    );
}

export default Header;